layout {
    pane size=1 borderless=true {
        plugin location="tab-bar"
    }

    pane split_direction="vertical" {
        // the main terminal area
    }

    // status bar at the very bottom (optional)
    pane size=1 borderless=true {
        plugin location="status-bar"
    }
}
